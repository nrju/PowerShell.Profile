# Nrju.PowerShell.Profile

The PowerShell module to import from the PowerShell `$PROFILE` file.

- Custom PowerShell prompt.

- `PSReadLine` module options.

- Argument tab completion for the command-line applications:

  - `git`;

  - `gh` (GitHub CLI);

  - `dotnet`;

  - `docker`;

  - `winget`
